pub async fn update(value: i32) -> i32 {
    return value;
}
@state(version = 1)
class EnemyBrain {
    mut phase: i32,
    mut legacy_target: i32,
}
@state(version = 1)
class StableBrain {
    mut phase: i32,
}
