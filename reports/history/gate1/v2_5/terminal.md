# Gate 1 v2.5 Terminal Record

Gate 1 v2.5 is `STRUCTURAL_CLOSURE_FAILED` and is not decision-usable. Its three apparatus-valid formal executions produced stable H1/H2/H3 `FAIL` outcomes and stable H2 semantic/allocation projections, while both performance comparisons were legitimately `INCONCLUSIVE`. Raw-to-Gate rebuild passed, but the frozen Gate generator incorrectly mapped legal `INCONCLUSIVE` outcomes to Contract `FAIL`; D2.5, R2.5, F2.5, Receipt, finalization, and push do not exist.
