# Gate 1 v2.8 Terminal Record

Gate 1 v2.8 is **SEMANTICALLY_INSUFFICIENT / INCOMPLETE**.

Its I2.8 → E2.8 → D2.8 → R2.8 → F2.8 topology and Raw evidence are preserved
unchanged. The recorded `UNVERIFIABLE_WITHIN_MVR` decision is not authorized because
the decision evaluator derived Stable Core Failure from one representative sub-Gate
per hypothesis instead of the three per-run aggregate hypothesis outcomes.

The structurally valid v2.8 Receipt therefore does not establish decision-semantic
sufficiency. Gate 1 v2.9 supersedes v2.8 and must independently freeze, execute, derive,
receipt, and finalize the corrected semantics.
