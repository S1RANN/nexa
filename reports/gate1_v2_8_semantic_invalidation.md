# Gate 1 v2.8 Semantic Invalidation

Gate 1 v2.8 is **SEMANTICALLY_INSUFFICIENT / INCOMPLETE** and its recorded
`UNVERIFIABLE_WITHIN_MVR` product decision is unauthorized.

The formal Raw evidence remains valid and immutable. The defect is confined to decision
derivation: Stable Core Failure was tested against H1 equivalence, H2 configuration, and
H3 migration sub-Gates rather than the aggregate H1, H2, and H3 outcomes for each of the
three formal executions. Those representative sub-Gates passed while their aggregate
hypotheses failed, so the evaluator incorrectly produced an empty stable-failure set.

The v2.8 Receipt is structurally valid but semantically insufficient because it recomputed
the same defective evaluator. Gate 1 v2.9 supersedes this decision record.
