# Gate 1 v1 Invalidation

Gate 1 v1 is retained as historical, auditable evidence, but its `HOLD` decision is no longer a
current project decision. The apparatus accepted a dirty postflight, shared processes across H1
and H3, emitted constant H1 edit metrics, inferred H2 invariants, supplemented H3 matrices, and
unconditionally marked contracts passed.

Status: **INVALID_APPARATUS**

Decision usable: **no**
Superseded by: **gate1-v2.1**

The original result, raw artifacts, and receipt remain unchanged so the failure can be audited.
